/**
 * LocalStorage keys used across the application.
 * Centralizing these constants prevents typos and ensures consistency.
 */

/**
 * Key used to store authentication state in localStorage.
 * Used by both Zustand persist middleware and axios interceptor.
 */
export const AUTH_STORAGE_KEY = "ubs-monitoring-auth";
